# Backup design

L'ancienne version de la landing page, avant le passage design inspire Phidias/propfirm, correspond au commit Git `2e76e70`.

Si le nouveau rendu ne convient pas, il suffit de repartir de ce commit pour recuperer l'ancien `styles.css` et l'ancien affichage public.
