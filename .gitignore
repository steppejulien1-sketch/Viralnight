node_modules/
dist/
.env.local
.env.*.local

.vercel
.env*

# Captures regenerables (outils/pages_b2b.cjs)
outils/pages
outils/pages-375
outils/pages-app
outils/pages-admin
outils/pages-internes
outils/pages-club
outils/pages-clubbeur
# Captures d'autres applis, tirees du Play Store par
# outils/references_play.cjs. Elles appartiennent a leurs editeurs :
# elles servent a REGARDER, jamais a etre recopiees, et n'entrent pas
# dans le depot.
outils/references
# La planche assemblee : regenerable par outils/planche_ecrans.py, et
# elle pese 843 Ko de JPEG en base64.
outils/noctify-ecrans.html

# Build de l'appli mobile (regenere par npm run build:mobile)
dist-mobile/

# Capacitor : le dossier ios/ lui-meme SE COMMIT (il porte la signature,
# les droits, Info.plist). Seuls ses artefacts de compilation sont ignores.
ios/App/Pods/
ios/App/build/
ios/App/App/public/
ios/App/Podfile.lock

# Note de reprise entre deux sessions. Snapshot date, pas de la doc :
# la doc durable vit dans CLAUDE.md et MOBILE.md.
REPRISE.md
